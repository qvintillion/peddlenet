# Vercel Production Environment Variables
# Used for Vercel production deployments

# Production WebSocket server on Google Cloud Run (same as before)
NEXT_PUBLIC_SIGNALING_SERVER=wss://peddlenet-websocket-server-hfttiarlja-uc.a.run.app

# Build target for Vercel
BUILD_TARGET=vercel-production

# Vercel automatically sets VERCEL=1 in production
# Our code will detect this and use Vercel API routes

# Firebase project (for any Firebase features if needed)
FIREBASE_PROJECT_ID=festival-chat-peddlenet

# Admin Dashboard Authentication (same as before)
# Default credentials: th3p3ddl3r / letsmakeatrade
# Uncomment and modify to change:
# ADMIN_USERNAME=your_custom_username
# ADMIN_PASSWORD=your_custom_password
