'use client';\n\nimport { MeshNetworkTest } from './admin/MeshNetworkTest';\n\n// Example of how to add mesh testing to any page\nexport function PageWithMeshTesting({ children }: { children: React.ReactNode }) {\n  return (\n    <>\n      {children}\n      \n      {/* Add mesh network testing widget to any page */}\n      <MeshNetworkTest \n        roomId=\"test-room\" \n        displayName=\"Test-User\"\n        onTestResult={(result) => {\n          console.log('🌐 Mesh test result:', result);\n          \n          // You can also send results to analytics, etc.\n          if (result.mesh?.p2pConnected) {\n            console.log('✅ P2P is working!');\n          }\n        }}\n      />\n    </>\n  );\n}\n\n// Usage example in a page component:\n/*\nexport default function ChatPage() {\n  return (\n    <PageWithMeshTesting>\n      <div className=\"chat-interface\">\n        {/* Your existing chat UI */}\n      </div>\n    </PageWithMeshTesting>\n  );\n}\n*/\n